import 'package:flutter/material.dart';

class MaterialsAccountingPage extends StatelessWidget {
  const MaterialsAccountingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
