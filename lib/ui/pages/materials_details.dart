import 'package:flutter/material.dart';

class MaterialsDetailsListPage extends StatelessWidget {
  const MaterialsDetailsListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
